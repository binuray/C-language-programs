//gets and puts function in c program
#include<stdio.h>
void main()
{
    char str1[50],str2[50];
    puts("Enter first string:\n");
    gets(str1);
    puts("Enter second string:\n");
    gets(str2);
     puts(str1);
      puts(str2);
}